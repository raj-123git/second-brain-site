{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "python ../scripts/build-knowledge.py && astro build && bash ../scripts/leak-check.sh",
    "preview": "astro preview",
    "astro": "astro",
    "leak-check": "bash ../scripts/leak-check.sh"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.1"
  },
  "allowScripts": {
    "esbuild": true
  }
}
